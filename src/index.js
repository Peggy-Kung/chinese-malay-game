import React from 'react';
import ReactDOM from 'react-dom/client';
import ChineseMalayGame from './ChineseMalayGame';
import './index.css';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <ChineseMalayGame />
  </React.StrictMode>
);